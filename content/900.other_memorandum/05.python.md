---
title: Pythonについて
description: Pythonの備忘録です。
navigation: true
draft: false
date: 2026-06-14T07:00:00+09:00
---

## Python HTTP通信で情報取得する

`python`がインストールできていれば、以下でOK。

```python
import sys
import json 
import urllib.request

## 取得したい情報のURL
url = "取得したい情報のURL"
header = {
  # 例えば header に仕込む key-value
  'key': 'value', 
  # 例えば header に仕込む 認証情報
  'Authorization': 'Bearer <YOUR TOKEN>'
}
req = urllib.request.Request(url, headers = header)

with urllib.request.urlopen(req) as res:
    body = json.load(res)
    print(body)

```

### 参考

* [Python の HTTP クライアントは urllib.request で十分 #Python - Qiita](https://qiita.com/hoto17296/items/8fcf55cc6cd823a18217)